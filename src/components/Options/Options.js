import React from 'react';

const Options = ({options}) => {
    console.log(options);
    const {options} = op;
    return (
        <div>
            <input type="radio" ></input>{options}
        </div>
    );
};

export default Options;